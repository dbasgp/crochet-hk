# crochet.com.hk

This domain has moved: all traffic 301-redirects to https://mintbox.hk via a Render route rule (`/*` → mintbox.hk).

The old site files were removed 2026-07-16 because Render serves real files before route rules — with index.html present, `/` never redirected.
